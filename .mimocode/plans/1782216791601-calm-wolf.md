# Plan: Commit Refactoring Changes

## Context
Two rounds of surgical refactoring have been completed on the arena_forge project:
- Round 1: Basic cleanup (dead code removal, duplicate elimination, config fixes)
- Round 2: Deep cleanup (dispatch consolidation, reverse dependency fix, circular dependency elimination, naming unification)

All 247 tests pass. Changes span ~75 files with net deletion of ~200+ lines.

## Commit Strategy

Single commit with a comprehensive message covering all changes, following the repo's lowercase commit style.

### Files to Stage

**Deleted files (7):**
- `cmp_sense/.settings` (already staged)
- `arena_forge/adapters/sublime/run_history.py`
- `arena_forge/adapters/sublime/run_panel/action_handlers.py`
- `arena_forge/adapters/sublime/run_panel/dispatch.py`
- `highlight_assets/test_interface.py`
- `plugin_support/process_manager.py`
- `arena_forge/adapters/sublime/command_action_catalog.py`
- `arena_forge/adapters/sublime/run_panel/command_mixin.py`
- `tests/test_run_panel_command_mixin.py`
- `settings_plugin.py`

**New files (2):**
- `arena_forge/adapters/sublime/run_panel/persistence.py`
- `arena_forge/adapters/providers/base.py`

**Modified files (~65):**
- All files listed in `git status` as modified

### Commit Message

```
refactor: surgical cleanup and architecture improvements

Round 1 - dead code and duplication:
- merge RunSessionService into SessionService (was never used)
- inline coerce_verdict, delete run_history.py
- delete dead plugin_support/process_manager.py re-export
- delete dead highlight_assets/test_interface.py
- clean debug_backends: remove debug prints, fix typos
- fix operations.py unreachable branch
- fix outdated ruff file paths in pyproject.toml
- clean number_splitter.py commented code

Round 2 - structural improvements:
- merge dispatch.py + action_handlers.py into commands.py
- handler dict now built once at module level (not per-call)
- simplify run() to **kwargs
- eliminate core→adapters reverse dependency via lazy import
- extract persistence.py to break session_actions↔state circular dep
- encapsulate settings_bridge globals in SettingsContext
- add PanelTestState aliases (input_text, accepted_outputs, rejected_outputs)
- migrate all consumers to new naming
- unify commands.py indentation to 4 spaces
- delete cmp_sense/ personal archive from repo
```

## Verification

1. Run `python -m pytest tests/ -x -q` → expect 247 passed
2. Run `python -m ruff check arena_forge/` → expect clean
3. Verify git status shows expected changes
